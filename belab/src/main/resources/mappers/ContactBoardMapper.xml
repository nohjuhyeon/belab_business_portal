<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.belab.co.kr.ConTact.dao.ConcatBoardMapper">

    <!-- 모든 게시글 조회 -->
    <select id="selectAllBoards" resultType="com.belab.co.kr.ConTact.vo.ContactBoardVO">
        SELECT
            dashboard_id,
            user_id,
            title,
            content,
            created_at,
            updated_at
        FROM contact_info_board
        ORDER BY created_at DESC
    </select>

    <!-- 특정 게시글 조회 -->
    <select id="selectBoardById" resultType="com.belab.co.kr.ConTact.vo.ContactBoardVO">
        SELECT
            dashboard_id,
            user_id,
            title,
            content,
            created_at,
            updated_at
        FROM contact_info_board
        WHERE dashboard_id = #{dashboard_id}
    </select>

    <!-- 게시글 등록 -->
    <insert id="insertBoard" parameterType="com.belab.co.kr.ConTact.vo.ContactBoardVO">
        INSERT INTO contact_info_board (
            user_id,
            title,
            content,
            created_at,
            updated_at
        )
        VALUES (
                   #{user_id},
                   #{title},
                   #{content},
                   NOW(),
                   NOW()
               )
    </insert>

    <!-- 게시글 수정 -->
    <update id="updateBoard" parameterType="com.belab.co.kr.ConTact.vo.ContactBoardVO">
        UPDATE contact_info_board
        SET
            title = #{title},
            content = #{content},
            updated_at = NOW()
        WHERE dashboard_id = #{dashboard_id}
    </update>

    <!-- 게시글 삭제 -->
    <delete id="deleteBoard" parameterType="int">
        DELETE FROM contact_info_board
        WHERE dashboard_id = #{dashboard_id}
    </delete>

</mapper>
